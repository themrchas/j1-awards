import { DisplayColTotalPipe } from './display-col-total.pipe';

describe('DisplayColTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayColTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
