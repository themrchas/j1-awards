import { DisplayRowTotalPipe } from './display-row-total.pipe';

describe('DisplayRowTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRowTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
